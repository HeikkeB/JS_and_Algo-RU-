function merge(arr, buffer, start, mid, end) {
	// см. 6.1
  ...
	
	// см. 6.2
    let l = start;
    let r = mid + 1;
    let i = start;
	
	// см. 6.3
    while (...) {
         if () {
            ...
    	    l++
         } else {
          ...
          r++
         }
         i++;
    }
	
	// см. 6.4
  while (..) {
       ...
       l++;
       i++;
    }
	
	// см. 6.5
  while (r < end + 1) {
       ...
       r++;
       i++;
  }
}
