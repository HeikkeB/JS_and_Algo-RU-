function partition(arr, start = 0, end = arr.length - 1) {
  const pivotValue = ...;
  
	let pivotIndex = ...;

  for (...) {
    if (...) {
			...
    }
  }

  swap(...);

  return pivotIndex;
}

function swap(arr, i, j) {
  const tmp = arr[i];
  arr[i] = arr[j];
  arr[j] = tmp;
}