function bubbleSort(arr) {
    ...
    while (...) {
        ...
        for (let i = 0; i < arr.length - 1; i++) {
            ...
        }
    }

    return arr;
}

function swap(arr, i, j) {
    const tmp = arr[i];

    arr[i] = arr[j];
    arr[j] = tmp;
}